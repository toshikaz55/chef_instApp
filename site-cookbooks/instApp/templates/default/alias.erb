alias ls="ls -F --color"
alias la="ls -a"
alias ll="ls -lhrt"

alias du="du -h"
alias df="df -h"

alias su="su -l"

if [ $SHLVL = 1 ]; then
    alias tmux="tmux attach || tmux new-session \; source-file ~/.tmux_session"
fi

alias opensb='open -a "sublime text 2"'
