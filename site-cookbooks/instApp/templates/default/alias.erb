alias ls="ls -F -G"                                                                                                                                                    
alias la="ls -a"                                                                                                                                                       
alias ll="ls -lhrt"

alias du="du -h"
alias df="df -h"

alias su="su -l"

alias python="source ~/works/python27/bin/activate;\python"

if [ $SHLVL = 1 ]; then
    alias tmux="tmux attach || tmux new-session \; source-file ~/.tmux_session"
fi

alias opensb='open -a "sublime text 2"'
